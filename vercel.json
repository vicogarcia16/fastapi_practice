{
    "devCommand": "uvicorn main:app --host 0.0.0.0 --port 3000",
    "buildCommand": "uvicorn main:app --host 0.0.0.0 --port 3000",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "main.py"
        }
    ]
}