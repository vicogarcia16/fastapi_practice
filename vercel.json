{
    "devCommand": "uvicorn vercel:app --host 0.0.0.0 --port 3000",
    "buildCommand": "uvicorn vercel:app --host 0.0.0.0 --port 3000",
    "framework": "fastapi",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "main.py"
        }
    ]
}